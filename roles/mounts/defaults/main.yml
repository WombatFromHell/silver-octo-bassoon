---
# Default values for mounts role

mounts_data1_path: "{{ mounts_base_path }}/data1"

# Users whose container storage gets a bind from data1/user-containers/<user>;
# defaults to the connecting user. Override per-host via host_vars if needed.
mounts_user_container_binds: ["{{ ansible_user_id }}"]
